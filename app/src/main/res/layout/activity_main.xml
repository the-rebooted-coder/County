<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    android:keepScreenOn="true"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/trainTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="160dp"
        android:fontFamily="@font/p_sans_b"
        android:text="🚅 Time to Go"
        android:textColor="#FF0099CC"
        android:textSize="34sp" />

    <com.tsuryo.androidcountdown.Counter
        android:id="@+id/counter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        app:custom_font="@font/p_sans"
        app:max_time_unit="DAY"
        app:text_color="#FFE0F3FF"
        app:text_size="32sp"
        app:textual_description="true" />
    <com.google.android.material.button.MaterialButton
        android:id="@+id/zenModeActiviate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/zenmode"
        android:fontFamily="@font/p_sans"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="40dp"
        android:layout_alignParentBottom="true"
        android:backgroundTint="#00C3C3C3" />

</RelativeLayout>